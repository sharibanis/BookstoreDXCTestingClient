CREATE TABLE books (
	ISBN bigint not null, 
	title varchar(255), 
	authors authors ARRAY[], 
	year int, 
	price DOUBLE, 
	genre varchar(255), 
	primary key (isbn)
)

CREATE TABLE authors (
	name varchar(255), 
	birthday varchar(255), 
	primary key (name)
)
 